<div class="map"></div>
<div class="container d-flex justify-content-center flex-wrap">
  <%= render "shared/search" %>
  <% @spaces.each do |space| %>
    <%= render "shared/card", space: space, delete: false %>
  <% end %>
</div>
